database:
  type: mysql
  schema: mydb
  user: root
  password: root
  host: mysql
  port: 3306

cdc_log:
  table: cdc_log

tables:
  - name: orders
    pk: id
  - name: users
    pk: id

cdc_server:
  offset_file: offset.txt
  publisher_addr: localhost:9092
  publisher_config: /config.yaml
